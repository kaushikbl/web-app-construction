apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-db
  namespace: dev

spec:
  replicas: 1
  revisionHistoryLimit: 2

  selector:
    matchLabels:
      app: mongo-db

  template:
    metadata:
      labels:
        app: mongo-db

    spec:
      containers:
        - name: mongo-db
          image: mongo:6.0
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"

          ports:
            - containerPort: 27017

          volumeMounts:
            - name: mongo-db-storage
              mountPath: /data/db

      volumes:
        - name: mongo-db-storage
          persistentVolumeClaim:
            claimName: mongo-pvc
